{
  "name": "codex-proxy",
  "version": "1.0.0",
  "description": "Codex CLI 多模型代理 — 将 Codex CLI 请求转换为国产大模型 API 格式",
  "main": "dist/main/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/cchenbin042/cli-proxy"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc && set VITE_DEV_SERVER_URL=http://localhost:1420 && electron .",
    "start": "electron .",
    "pack": "electron-builder --dir",
    "dist": "electron-builder",
    "dist:win": "electron-builder --win"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "electron": "^33.0.0",
    "electron-builder": "^25.0.0",
    "typescript": "^5.6.0"
  },
  "author": "cli-proxy",
  "license": "MIT",
  "dependencies": {
    "electron-updater": "^6.8.3",
    "js-yaml": "^4.1.1"
  }
}
